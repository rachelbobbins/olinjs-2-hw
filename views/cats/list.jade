table.table.table-striped
  thead
    tr
      th Name
      th Age
      th Colors
  tbody
    each cat in cats
      tr
        td #{cat.name}
        td #{cat.age}
        td #{cat.colors.join(', ')}